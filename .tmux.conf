# ~/.tmux.conf — nb30
#
# Standard-Keybinds, absichtlich unangetastet: Prefix C-b, teilen mit % und ",
# Panes mit den Pfeiltasten, Zoom mit z, Fenster ab 0. Damit passt jede Anleitung
# im Netz, es gilt dasselbe am Framework und am VPS (der gar keine Config hat),
# und C-a bleibt in der Shell der Sprung an den Zeilenanfang.
#
# FARBEN — hier steht bewusst KEIN Hex.
# Die Linux-Textkonsole kann nur 16 Farben. Hex-Werte (#2e3440 usw.) werden dort
# beliebig gemappt, das sieht schmutzig aus. Mit ANSI-Indizes (colour0..15) folgt
# tmux dagegen der Palette des Terminals, in dem es gerade laeuft: in der TTY die
# Standardfarben, in einem Terminal mit Nord-Palette automatisch Nord.
# Eine Config, ueberall stimmig — und nichts, was am Framebuffer haengenbleibt.

### Grundlagen ---------------------------------------------------------------
# screen-256color, nicht tmux-256color: letzteres braucht ein terminfo, das auf
# einem frischen Void fehlen kann — dann startet tmux gar nicht.
set -g default-terminal "screen-256color"
set -g mouse on
set -g history-limit 10000
set -sg escape-time 10
set -g focus-events on

# status-interval steht ganz unten, NACH dem Laden der Plugins.
# Grund: tmux-sensible setzt es auf 5 und wuerde einen Wert hier stillschweigend
# ueberschreiben. Am NB30 gemessen: die Leiste lief dreimal so oft wie gedacht.

### Bequeme Griffe — ZUSAETZLICH, nichts wird umbelegt --------------------
# Der Standard bleibt vollstaendig erhalten: % und " teilen weiter, die
# Pfeiltasten wechseln weiter das Feld. Kein 'unbind'. Damit passt jede
# Anleitung im Netz, und auf einem fremden Rechner steht man nicht dumm da.
#
# Das hier liegt nur obendrauf:
#   |  und  -   teilen, und der Strich zeigt, wo geschnitten wird
#   h j k l     Feld wechseln (wie in Vim)
#   H J K L     Feldgrenze verschieben, wiederholbar (-r: Taste gedrueckt lassen)
#
# Der eigentliche Alltagsgewinn ist -c "#{pane_current_path}": ein neues Feld
# geht im SELBEN Verzeichnis auf, statt im Home zu landen.
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
bind c new-window      -c "#{pane_current_path}"

bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# r laedt die Config neu. r ist im Standard unbelegt.
bind r source-file ~/.tmux.conf \; display "tmux.conf neu geladen"


### Statusleiste — dunkelblaue Leiste, weisse Schrift ----------------------
# DREI Grenzen der Linux-Textkonsole, alle real eingetreten:
#
#   1. Der HINTERGRUND kann nur 8 Farben (colour0-7). Die hellen Varianten
#      (colour8-15) gibt es dort nur fuer Schrift, nicht fuer Flaechen.
#      Ein Blaugrau wie in Nord ist damit unmoeglich — colour4 (dunkelblau)
#      ist die einzige blaue Flaeche, die die TTY hergibt.
#   2. Helle Flaechen (cyan colour6) tragen keinen hellen Text. Der Grund muss
#      dunkel sein, dann steht Weiss gut darauf.
#   3. 'bold' hellt in der TTY die VORDERGRUNDFARBE auf. Ein fettes fg=colour0
#      ist nicht schwarz, sondern grau bis weiss — auf hellem Grund
#      verschwindet die Schrift ganz. Also nie 'bold' auf dunklem Vordergrund.
set -g status on
set -g status-position bottom
set -g status-justify left
set -g status-style "bg=colour4,fg=colour7"

# POWERLINE-TRENNER — die schraegen Pfeile zwischen den Segmenten.
# Die Glyphen (U+E0B0 rechts, U+E0B2 links) stecken im Konsolenfont
# TamzenForPowerline10x20. Steht in /etc/rc.conf ein anderer Font, erscheinen
# hier leere Kaestchen — dann: tty-font --setzen TamzenForPowerline10x20
#
# Der Trick dabei: der Pfeil traegt die Farbe des VORHERIGEN Segments als
# Vordergrund und die des naechsten als Hintergrund. So fliesst das eine
# Segment ins andere.
#
# Kein 'bold' auf dunklem Vordergrund — in der TTY hellt bold den Vordergrund
# auf, schwarze Schrift wuerde grau und auf hellem Grund unsichtbar.

# Links: Session auf hellem Grund, dann Uebergang ins Blau der Leiste.
set -g status-left "#[bg=colour7,fg=colour0] #S #[fg=colour7,bg=colour4]#[fg=colour15] #{prefix_highlight}"
set -g status-left-length 30

# Rechts: CPU · RAM · Temperatur · Akku (mit Restzeit) · Datum · Uhr
#
# Das DATUM kommt aus tmux selbst (%a %d.%m.), nicht aus einem Skript — tmux
# kann Zeitformate nativ, das kostet keinen einzigen Prozess.
#
# Alles andere ueber ~/.local/bin/tmux-status, das nur Dateien liest
# (/proc/stat, /proc/meminfo, /sys/class/power_supply, /sys/class/thermal).
# Bewusst KEIN tmux-cpu / tmux-battery: am NB30 gemessen brauchen die Plugins
# 0,39 s pro Aktualisierung, das eigene Skript 0,05 s. Und tmux-cpu meldete auf
# dem Zweikern-Atom 107 % Auslastung, weil es ohne 'iostat' auf 'ps' zurueck-
# faellt und ueber die Kerne summiert. Teuer UND falsch.
#
# Keine Icons: ein PSF-Konsolenfont hat nur ein paar hundert Glyphen, Emoji
# erscheinen dort als leere Kaestchen.
# Rechts: Werte auf dem Blau der Leiste, dann Uebergang auf hellen Grund fuer
# Datum und Uhr. Der linke Pfeil (U+E0B2) traegt die Farbe des KOMMENDEN
# Segments als Vordergrund.
set -g status-right "#[fg=colour15]#(tmux-status cpu) CPU  #(tmux-status ram) RAM  #(tmux-status temp)  #(tmux-status akku) #[fg=colour7,bg=colour4]#[bg=colour7,fg=colour0] %a %d.%m.  %H:%M "
set -g status-right-length 80

# Aktives Fenster: weiss + fett. Uebrige: gedaempft, aber noch lesbar.
setw -g window-status-format         "#[fg=colour7]  #I #W  "
setw -g window-status-current-format "#[fg=colour15,bold]  #I #W  "
setw -g window-status-style          "bg=colour4"
setw -g window-status-separator ""

# Panes / Nachrichten
set -g pane-border-style        "fg=colour8"
set -g pane-active-border-style "fg=colour7"
set -g message-style "bg=colour4,fg=colour15,bold"
set -g mode-style    "bg=colour4,fg=colour15"

### Plugins (TPM) — Prefix(C-b) + I zum Installieren ------------------------
# install.sh holt TPM; ohne TPM meldet die letzte Zeile nur einen harmlosen Fehler.
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'          # Sessions speichern
set -g @plugin 'tmux-plugins/tmux-continuum'          # automatisch sichern/wiederherstellen
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'   # zeigt, wann tmux zuhoert
set -g @plugin 'tmux-plugins/tmux-logging'           # Ausgabe mitschreiben
set -g @plugin 'sainnhe/tmux-fzf'                    # Suchliste: Strg+B, dann F

# KEIN tmux-menus: das Menue ist oben von Hand gebaut (display-menu), auf
# DEUTSCH. Das Plugin ist englisch — es zu uebersetzen hiesse, es zu forken,
# und beim naechsten Update waere die Arbeit weg.
#
# CPU/RAM/Akku bewusst OHNE Plugin — siehe Statusleiste oben und bin/tmux-status.

set -g @continuum-restore 'on'

# --- tmux-fzf: Sessions/Fenster/Felder ueber eine Suchliste ----------------
# Strg+B, dann grosses F. Braucht das Programm 'fzf'.
# 'f' bleibt frei fuer den Standard (find-window).
TMUX_FZF_LAUNCH_KEY="F"

# --- tmux-logging ---------------------------------------------------------
# Strg+B, dann grosses P schreibt die Ausgabe des Feldes in eine Datei mit.
# Genau das, was beim Browser-Debugging am 12.7.2026 fuenfmal von Hand
# zusammengebastelt wurde ('browser orf.at > /tmp/b3.log 2>&1').
# Weitere Griffe: Alt+P sichert die bisherige Ausgabe, Alt+C leert sie.
set -g @logging-path "$HOME/tmux-logs"
set -g @screen-capture-path "$HOME/tmux-logs"
set -g @save-complete-history-path "$HOME/tmux-logs"

# Prefix-Anzeige: sichtbar, aber ohne Farbfehler.
# Kein 'bold' auf dunklem Vordergrund — in der TTY hellt bold den Vordergrund
# auf, fettes Schwarz waere grau und auf hellem Grund unsichtbar.
set -g @prefix_highlight_fg 'colour0'
set -g @prefix_highlight_bg 'colour7'
set -g @prefix_highlight_prefix_prompt 'TASTE'
set -g @prefix_highlight_show_copy_mode 'on'
set -g @prefix_highlight_copy_prompt 'KOPIEREN'
set -g @prefix_highlight_copy_mode_attr 'fg=colour0,bg=colour7'
set -g @prefix_highlight_empty_prompt ''

run '~/.tmux/plugins/tpm/tpm'

### NACH den Plugins — sonst wird es ueberschrieben ------------------------
# WICHTIG: Alles, was ein Plugin ebenfalls belegen koennte, gehoert HINTER den
# tpm-Aufruf. Sonst gewinnt das Plugin. Genau das ist zweimal passiert:
# tmux-sensible ueberschrieb status-interval, tmux-menus die Taste M.

### Menue — Strg+B, dann M ------------------------------------------------
# Eigenes Menue statt des Plugins 'tmux-menus'. Gruende:
#   1. Es ist DEUTSCH. Das Plugin ist englisch, und es zu uebersetzen hiesse,
#      es zu forken — beim naechsten Update waere die Arbeit weg.
#   2. Es zeigt genau die Griffe, die hier eingerichtet sind, und keine 200
#      anderen.
#   3. tmux kann Menues selbst (display-menu). Kein Plugin, nichts, was bricht.
#
# Die Buchstaben in der zweiten Spalte sind die Kuerzel IM Menue.
#
# Ueberschriften beginnen mit EINEM Bindestrich — display-menu zeigt solche
# Eintraege ausgegraut und nicht anwaehlbar. Mit ZWEI Bindestrichen haelt tmux
# es fuer eine Befehlsoption und bricht ab ("invalid flag --").
bind M display-menu -T "#[align=centre]tmux" -x C -y C \
  "-Teilen" "" "" \
  "senkrecht teilen"            t "split-window -h -c '#{pane_current_path}'" \
  "waagrecht teilen"            w "split-window -v -c '#{pane_current_path}'" \
  "Feld gross/klein (Zoom)"     z "resize-pane -Z" \
  "Feld schliessen"             x "confirm-before -p 'Feld schliessen? (y/n)' kill-pane" \
  "" \
  "-Fenster" "" "" \
  "neues Fenster"               n "new-window -c '#{pane_current_path}'" \
  "Fenster umbenennen"          u "command-prompt -I '#W' { rename-window '%%' }" \
  "Fenster waehlen"             f "choose-tree -Zw" \
  "Fenster schliessen"          q "confirm-before -p 'Fenster schliessen? (y/n)' kill-window" \
  "" \
  "-Sitzung" "" "" \
  "aussteigen (laeuft weiter)"  d "detach-client" \
  "Sitzung waehlen"             s "choose-tree -Zs" \
  "Sitzung umbenennen"          r "command-prompt -I '#S' { rename-session '%%' }" \
  "" \
  "-Sonstiges" "" "" \
  "zurueckblaettern"            b "copy-mode" \
  "Ausgabe mitschreiben"        m "run-shell '~/.tmux/plugins/tmux-logging/scripts/toggle_logging.sh'" \
  "Spickzettel"                 h "new-window -n hilfe 'tmux-hilfe; read -p \"\" _'" \
  "alle Tastenkombinationen"    ? "list-keys -N" \
  "" \
  "Menue schliessen"            Escape ""

### status-interval — ebenfalls nach den Plugins ---------------------------
# tmux-sensible setzt status-interval auf 5. Auf dem Atom N450 heisst das:
# alle 5 Sekunden drei Shell-Prozesse fuer die Anzeige. 15 Sekunden reichen
# voellig und kosten ein Drittel. Steht diese Zeile weiter oben, kassiert
# sensible sie beim Laden — genau das ist hier passiert.
set -g status-interval 15
